# Samba
Minimal Samba daemon.

## Purpose
This Docker image is inteded to be a base image.
Users need to be added and the configuration needs to be altered (see the [man page](https://www.samba.org/samba/docs/current/man-html/smb.conf.5.html))
